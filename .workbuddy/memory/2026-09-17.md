# 2026-09-17 工作日志

## 家庭成员 tab 按钮守卫（用户反馈：未保存基本信息时不应有"添加家庭成员"按钮）
- switchCrTab 切到 paneMember 时调 updateMemberTabState()：familySaved=false → memBar 隐藏 + 空态文案"请先完善并保存家庭基本信息后再添加家庭成员"；true → 按钮显示 + 常规空态文案。保存后 tipGotoMember 的 switchCrTab 自然触发刷新，无需额外处理。

## 添加家庭成员弹框：点行展开录入区 → 保存新增成员行（用户截图需求）
- **双模式分流**：memberMode（owner=选户主：点行直接 pickMember 带入并关弹框；member=添加家庭成员：点行 showMemberInfo 展开弹框内下方"家庭成员信息"录入区）。renderMemberTable 行 onclick 统一走 memberRowClick(no) 分流。
- **录入区 mmInfo**（modal-body 内、mCancelBar 之后）：蓝左边框标题 + 三列 mm-grid（性别/联系电话带出；身份证号带出、*与户主关系/*血缘关系下拉必填；家庭地址整行带出=findRegionPath(village)+detail）+ 右下 [保存][取消]。member 模式下 mCancelBar 隐藏（录入区自带取消）。
- **saveMemberRow**：校验两个必填下拉 → memTbody appendRow（序号递增、档案编号 genMemberNo、掌握=未掌握、操作=编辑(蓝)/注销(红) 可点）→ closeMemberModal → toast。cancelMemberRow 仅收起录入区；closeMemberModal/openMemberModal 均调 resetMemberInfo。
- 与户主行区别：户主行操作列 op-disabled 灰色不可点；普通成员行 op-act 蓝/红可点。

## 保存成功 → 提示弹框 → 家庭成员户主行（用户截图需求）
- **流程**：saveFamily()（校验户主已选）→ tipModal 小弹框（modal-sm 400px：提示标题/家庭档案建立成功！/[添加家庭成员]/[取消]）→ tipGotoMember() 关弹框 + renderOwnerRow() + switchCrTab 切"家庭成员"。
- **户主行**：mem-tbl 12 列（序号/姓名/档案编号/genMemberNo 随机 18 位/✎与户主关系=本人或户主/身份证号/性别/年龄按出生年算/联系电话/✎血缘关系=无血缘关系/常驻住址/掌握=未掌握/操作）；**操作列注销/删除为 op-disabled 灰色**（点击 toast「户主不可注销/删除」）。familySaved 标志防重复渲染；openCreateView 重置成员表为空态。
- **wrapper 新坑**：iframe src 中文百分号编码手写易错（"档"=%E6%A1%A3，写成 %E6%A3%93 变"椓"，页面加载失败 → openCreateView is not a function）；file:// 跨域拦截偶发，多数时候 --allow-file-access-from-files 够用，必要时加 --disable-web-security。

## 新建表单宽度与可编辑态修复（用户反馈：家庭地址列过宽/过窄、字段灰底禁编辑）
- **grid 等宽坑**：`repeat(3,1fr)` 的 `1fr`=minmax(auto,1fr)，长值输入框会撑宽所在列 → 一律用 `repeat(3,minmax(0,1fr))`。
- **input 不拉伸坑**：`.cascader` 原来不是 flex 容器，内部 `.cr-ctl{flex:1}` 不生效，input 停在固有宽度约 198px → 容器加 `display:flex`，输入框加 `min-width:0`。
- **禁编辑态**：户主姓名/证件号码/家庭地址的 readonly 属性全部移除（白底可编辑；点击行为保留——户主仍弹选择框、地址仍弹级联）；仅「家庭档案编号」保留 readonly 灰底（系统生成号）。JS 中 pickMember/clearOwner 的 readOnly 赋值同步清理。
- wrapper 验证日志模式：取 `getBoundingClientRect().width` 对比同行控件宽度 + 读 input.readOnly 布尔，一条日志同时验证两个维度。

## 户主选择弹框 + 个档带入 + 家庭地址级联
- **添加家庭成员弹框**（memberModal，样式同居民档案弹框规范）：两行筛选（姓名/身份证号 + 管辖网格/档案编号 + 重置/查询）、9 列档案表（点行选取）、动态分页、右下取消；memberPool 8 条与截图数据一致（王海云/测试×5/自动化测试用户/李大海/李文顺）。
- **个档带入**（pickMember）：户主姓名+⊗清除钮（syncOwnerIcons 切换放大镜/清除）、证件类型=居民身份证、证件号码由 readonly 转可改、性别映射男→男性、出生日期、家庭电话=联系电话、家庭地址=findRegionPath(村居委会) 全路径、详细地址。clearOwner 一键清空全部带入字段。
- **家庭地址级联**：select 换 cascader（readonly input + cs-panel 多列面板）；regionTree 省/市/区县/镇/村社区五级（江苏省→扬州市→江都区→仙女镇→东苑社区等全量，其余省 1 条示例链）；renderCs 按路径深度渲染列、csPick 逐级下钻/叶子提交（值=路径 join ' / '）；document click 关面板。
- **wrapper 调试经验**：headless 截图偶发"类已加但渲染未合成"假象（log 显示 class 生效但截图无遮罩），重跑即正常——用可见 log div（fixed 黄底）输出中间状态定位；级联逐级点击验证用 querySelectorAll('.cs-item') 按 textContent（去掉›箭头）匹配点击。

## 家庭档案"新建"改为页内视图切换（非弹框）
- 列表内容包进 `#listView`，新增 `#createView`（家庭档案信息）：cr-head（标题+返回）→ cr-tabs 下划线选项卡（家庭基本信息/家庭成员/精准扶贫）→ paneBase（家庭基本信息 3 列 cr-grid 表单 + 家庭社会经济情况 soc-tbl 选项表 + 备注 textarea 0/100 + 左下保存）→ paneMember/panePoor 占位。
- 细节：编号/证件号码 readonly 灰底；户主姓名为"请选择户主"+放大镜（点击 toast）；建档时间默认 2026-09-17；建档负责人预填 1814109564346885890；radio accent-color 主色，默认勾选 排风=无/饮水=自来水/厕所=卫生厕所；openCreateView/backCreateView 切换 display。
- **wrapper 踩坑**：中文文件名的 iframe.src 在属性里是百分号编码，`src.indexOf('家庭档案')` 永远不命中——改按"contentWindow.openCreateView 存在"探测目标 iframe（脱敏设置页无此函数，天然区分）。

## 家庭档案内容区风格对齐居民档案
- 结构重排：删 page-body/分离卡片版式 → **tabbar（家庭档案 active，dashboard 图标，嵌入模式由壳子隐藏）+ content > content-card 四段一体式白卡**（page-head h1+右侧按钮 border-bottom / filter-panel / table-wrap / page-foot border-top）。
- 样式逐项抄齐居民档案：filter-panel(padding:14px 12px 0 + #E8E8E8 底线)、fi-ctl 180×32 14px、fi-select 140px、btn-plain「展开 +」(替代 link-toggle)、page-foot 52px 顶线版式（pg-pager/pg-total b 主色）、表头 34→36px、行 hover 高亮（fam-row 用 .fam-row:hover 特异性避免盖掉 open 高亮；member-tbl 也加 hover）；label 去"："冒号；删除本地 .btn 覆盖改用框架公共 .btn。
- JS：toggleExtra 改 btnExpand.textContent('收起 −'/'展开 +')；新增 applyFilter()（查看按钮）。
- 验证：Edge headless 截图（?embed=1 直开 + wrap_family 壳子视图）均通过。

## 新建个人档案弹框布局改版（按参考图2）
- 布局重排：证件号行从左侧表单移到**右侧回显区顶部**（label + input(自适应宽,0/18) + 身份证读卡按钮），下方接浅蓝虚线回显卡；左侧表单收窄为单列 316px（证件类型/姓名/性别/民族/联系电话/出生日期）；**联系地址整行铺底**（modal-cols 之下的 fm-addr-row：label + 省市区下拉 186px + 详细地址 input flex:1）。
- CSS：`.modal` 920→860px；`.modal-body` 改 block + 新增 `.modal-cols` flex；新增 `.pv-side`（flex column，证件号行+卡片）；`.fm-addr-row`（整行铺底）；删除死样式 `.fm-row label.lb-auto`。
- 验证（Edge headless 截图）：空态与参考图一致；填写态 证件号 518987199909071220 → 自动解析 性别=女性/出生日期=1999-09-07，回显卡实时联动正常，字数计数 18/18、2/8 正常。

## 新增 家庭档案.html（家庭-成员两级主从表）
- 页面结构：页头（导出/新建）→ 两行筛选（管理机构/户主姓名/证件号/家庭档案编号；建档日期范围/详细地址/管理行政区/家庭状态默认"正常" + 重置/查看/展开▾ 隐藏第三行）→ 家庭主表 13 列（+切换图标/管理机构/户主姓名/家庭档案编号/户主证件类型/户主证件号码/户主出生日期/贫困情况/详细地址/建档医生/建档日期/建档机构/操作[编辑·查看·删除家庭档案(红)]）→ 成员子表（序号/姓名/与户主关系/性别/证件类型/证件号/出生日期/电话/地址/操作）嵌套在 `<tr class="member-block"><td colspan=13>` 内的独立 table；分页按截图"共 163594 条 / 1-6···8180"。
- 交互：+/- 切换成员块（首户默认展开且 fam-row.open 高亮）、筛选实时过滤、重置。
- 菜单：框架.js 中已有 `family-archives` 家庭档案项，本轮仅补 `url: '家庭档案.html'`。
- **踩坑**：filter 引用数据中不存在的 `status` 字段 → `undefined !== '正常'` 把全部行过滤掉显示空态；修复为 `(r.status || '正常') !== status`。另一个小坑：默认展开行的 +/- 图标与 open 高亮需在 renderTable 里按 open 状态同步生成，不能渲染后手补。

## 修复：壳子标签页"关不掉"
- 诊断（wrapper 实测）：closeTab 逻辑本身正常（click() 后标签 2→1），真因是 框架.css `.tab-close{opacity:0}`——关闭按钮平时完全透明，悬停才 0.5 且仅 12×12px，用户发现不了也点不中（点到标签主体变成 activate 切换）。
- 修复（框架.css）：`.tab-close` opacity 0→**0.45 常显**、尺寸 12→16px、hover 1；业务页静态标签栏（脱敏规则/居民档案列表）同步受益。
- 教训：交互控件不可见（opacity:0 等待 hover）是"功能坏了"的常见假象；诊断时先量化 computedStyle（wrapper 日志输出 opacity/尺寸）再下结论。

## 表格前 4 列横向冻结
- 实现：`.list-tbl th/td:nth-child(-n+4){position:sticky}`，left 按列宽累加（0/44/194/270px），td 补白底 `background:var(--bg-white)`（sticky 单元格必须不透明，否则滚动穿透），第 4 列右缘 `box-shadow:6px 0 8px -6px rgba(0,0,0,.2)` 分隔。
- **踩坑**：首轮把 th 一并写死 `z-index:2`——低于其他 th 的 z-index:5，横向滚动后后续表头（第 10 列起）正好覆盖在冻结表头上，表头与数据错位。修复：td z-index:2、th 单独提到 6（高于普通 th 的 5）。
- 验证：wrapper 设置 `.table-wrap`.scrollLeft=800 后 Edge headless 截图，前 4 列表头+数据均固定、与后续滚动列对齐。
- hover 行高亮规则 `.list-tbl tbody tr:hover td` 特异性 (0,2,3) 高于 sticky td 白底 (0,2,1)，悬停时冻结列也正常变色，无需额外处理。

## 表格列改版终版（用户要求保留原有列）
- 最终结构 **24 列**：前四列固定（序号/管理机构/姓名/证件号）+ 原有 10 列（证件类型/性别/年龄/电话号码/出生日期/重点人群类型绿徽标/是否合格徽标/现居住址/档案编号/管理行政区）+ 新增 9 列（行政区/责任医生/建档医生/建档日期/建档机构/档案完整度%/人群性质/居民状态绿点/银行状态橙点）+ 操作(130px)。
- 恢复了 .crowd-badge/.crowd-none/.qual-badge CSS；min-width 2302px；colspan 24。中间态"仅14列"方案被用户否掉——**用户明确：原有字段列一律保留，新增列是补充不是替换**。

## 居民档案列表表格列结构改版（对齐新截图）
- 表格 15 列 → **14 列**：序号/管理机构/姓名/证件号（前四列固定不动）+ 行政区(社区/村)/责任医生/建档医生/建档日期/建档机构/档案完整度(%)/人群性质/居民状态/银行状态/操作。
- 新列数据由现有字段派生：行政区=地址含"村"→村否则社区；责任医生=建档医生（医生池按行号轮转：冯仁卉/李蕾/朱劲松/敖鹏天/陆柳琳/王盛）；建档日期=r.created；建档机构=r.org；档案完整度=合格?100:0；人群性质=crowd 映射全称（65岁及以上老年人/高血压患者/2型糖尿病患者/0-6岁儿童），空 crowd 显示空；居民状态=绿点+正常；银行状态=橙点+未上传。
- 删除旧列：证件类型/性别/年龄/电话号码/出生日期/重点人群类型(绿色徽标)/是否合格(徽标)/现居住址/档案编号/管理行政区；删除 .crowd-badge/.crowd-none/.qual-badge CSS，新增 .st-dot 状态圆点（ok 绿 / warn 橙 #FF9500）。
- 宽度：min-width 1360px，操作列 96→130px（96px 会截断"更多▾"）；colspan 15→14。
- 筛选区未动（性别/出生日期等筛选仍作用于数据字段，虽无对应列）。
- 验证：Edge headless（profile 独立目录）截图两轮，第二轮确认操作列完整。

## 环境经验（重要）
- **agent-browser daemon 挂死**：今日连续 3 次 batch 命令挂起 8 分钟+ 无输出（杀残留 node daemon、去 networkidle 均无效）。`*> 重定向` 的日志文件在进程退出前读不到。
- **替代方案：Edge headless + wrapper 截图**（本次验证成功，推荐备用）：
  - 写 wrapper HTML：iframe 加载目标页 `?embed=1`，`f.onload` + `setTimeout(600)` 内 `f.contentWindow.openCreateModal()` / 填值（`el.value=v; el.dispatchEvent(new Event('input',{bubbles:true}))`）触发联动。
  - 命令：`msedge.exe --headless=new --disable-gpu --no-first-run --allow-file-access-from-files --user-data-dir=<独立profile> --virtual-time-budget=8000 --window-size=1280,900 --screenshot=<out.png> file:///<wrapper>`。
  - 坑1：必须加 `--allow-file-access-from-files`（file:// 父子 iframe 访问）；坑2：`--user-data-dir` 每次用**独立目录**，复用同一 profile 第二次截图会竞态失败（弹框未打开但页面正常）；坑3：wrapper 加 `window.onerror` 显示层便于排查。
  - Edge 路径：`C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe`。

## 家庭档案·新建页一屏化（15:53）
- 压缩家庭基本信息填报纵向密度：cr-body 10px / sec-title mb 8px / cr-grid 行距 10px / soc-tbl 行 padding 4-5px / 备注 textarea 46px / cr-foot 8px。1600×900 一屏完整（Edge headless 实测 FIT-OK），1440×810 略超 ~37px。

## 家庭成员行内编辑 + 公卫标准字典（16:18）
- 成员表操作列"编辑"由 toast 改为行内编辑：与户主关系/血缘关系两列变下拉（.mem-edit-sel 主色描边）、行加 .editing 高亮 #EDF5FF、操作列变 保存/取消；saveMemberEdit 校验非空后回写文本并 toast，cancelMemberEdit 按 data-rel/data-blood 还原；editingTr 全局保证同一时间仅一行编辑。户主行保持无编辑钮。
- 字典统一 JS 维护并与弹框同源：REL_DICT（GB/T 4761-2008 家庭关系代码归类：本人或户主/配偶/之子/之女/之父/之母/之祖父/之祖母/之孙子/之孙女/兄弟/姐妹/儿媳/女婿/其他亲属/非亲属）；BLOOD_DICT（WS 364.3 CV02.01.201 血缘关系代码：无血缘关系/直系血缘关系/三代以内的旁系血缘关系/三代以外的旁系血缘关系/不详/其他）。mmRel/mmBlood 改为 fillDict 动态填充（HTML 硬编码 option 已删）。血缘关系列宽 110→150px。
- 验证（Edge headless 双 wrapper：编辑态/保存态）：编辑行下拉预选当前值、保存后回写"之女/三代以内的旁系血缘关系"完整显示、操作列恢复编辑/注销；弹框下拉 17/7 个 option（含请选择）。

## 成员注销二次确认框（16:30）
- memberOpsHtml 注销按钮 toast → askCancelMember：#cancelModal（modal-sm 复用 tipModal 版式）文案「确认注销家庭成员「XX」吗？」+ cancel-sub 副文案，红色确定注销（新增页内 .btn-danger）+ 取消；confirmCancelMember 删行→重排剩余行序号→若空恢复空态→toast「已注销…数据已删除」；编辑态行点注销先自动退出编辑。户主行注销仍灰显不可点。
- **headless 截图坑（确定性）**：弹框 .modal-overlay.show 有 0.2s 淡入过渡，截图固定落在动画中段呈半透明（连续重跑同样）——wrapper 往 iframe 注入 `.modal-overlay{transition:none!important}` 即可完整渲染。
